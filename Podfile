# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'SampleForHandle' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
  
    pod 'Handle', :git => 'https://github.com/ribhu69/Handle.git', :commit =>  'ba595ee335e03f2f70e0535478fa34389478bb03'
    # Pods for YourProjectName
  
    # Add your pods here, example:
    # pod 'Alamofire', '~> 5.0'
    # pod 'Kingfisher', '~> 6.0'
    # pod 'SwiftyJSON', '~> 5.0'
  
    # Pods for testing
    # pod 'Quick', '~> 4.0'
    # pod 'Nimble', '~> 9.0'
  end
  
  # Post-install hooks
  # after_install do |installer|
  #   installer.pods_project.targets.each do |target|
  #     if target.name == 'FirebaseAnalytics' || target.name == 'Firebase'
  #       target.build_configurations.each do |config|
  #         config.build_settings['OTHER_LDFLAGS'] ||= ['$(inherited)']
  #         config.build_settings['OTHER_LDFLAGS'] << '-ObjC'
  #       end
  #     end
  #   end
  # end
  